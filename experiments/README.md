# Experimentos

Este directorio está reservado para suites de experimentos reproducibles del CLI.

Ejemplo futuro:

```text
experiments/
├── convex_baselines.yaml
├── attention_regularization.yaml
└── solver_comparison.yaml
```

Por ahora, los ejemplos ejecutables principales siguen en `examples/`.
